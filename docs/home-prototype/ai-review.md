# 独立AIレビュー

2026-09-08 JST。実装担当とは別のAIが `review-frontend` と `review-security` を使用してレビュー。レビュー対象は今回のHome試作全体とスマホ本文の視認性修正。アプリコードは編集していない。

## コードレビュー結果

コード上の修正必須指摘なし。視覚面の最終判定は候補1のマネージャー補助ブラウザ検証結果を受領後に追記する。

| 観点 | 結果・根拠 |
| --- | --- |
| R1 名前形成 | `nameTargets.js` が初期GPU位置を文字内へ配置。`nameLines` は `YuugouOhno` または `Yuugou` → `Ohno`。通常HTML h1を常設し、ローカルフォントでWebフォント待ちを避ける。 |
| R2 解放と本文 | `homeMotion.js` の進捗平滑化、`home-velocity.frag` の力の補間と速度・加速度上限を確認。位置を書き換える解放はない。今回の側方先行の力、深さによる描画サイズ補間は連続。本文遮蔽外の実描画ピクセル検査を追加して、GPU中心座標だけの検査不足を補っている。 |
| R3 滞在内ラッチ | `advanceRelease` は閾値到達後の解放を保持。Homeのvisitを動き設定の切替でも保持し、深い位置の初期化は解放済み分布になる。 |
| R4 通常閲覧 | HTMLを先に置いてからSceneを遅延import。通常リンク・実スクロールを使用し、canvasはpointer-events:none。本文は不透明背景を持ち魚の遮蔽を確保。 |
| R5 フォールバック | reduced-motionではSceneを起動しない。WebGL/float target/初回シェーダー/描画例外・context lossで破棄してHTMLを維持。 |
| R6 所有権・破棄 | Homeの世代番号で非同期importの古い起動を防止。disposeはRAF、Observer、visibility/context listener、mesh/material、target texture、GPUComputationRenderer、rendererを解放。resizeは既存uniformとcameraの更新でGPU状態を作り直さない。今回変更はHome専用GLSL・mesh/layoutに限定。 |
| R7 検証 | release境界・ラッチ・時間差・文字順、16組のリサイズ投影、魚の投影サイズのテストを確認。ブラウザ検査に本文到達時/30秒後の実描画検査と画像保存を追加。 |
| セキュリティ | 新規の外部入力・機密情報・送信先・依存なし。固定HTML、外部リンクのnoopener/noreferrerを維持。今回差分について追加のセキュリティ指摘なし。 |

## 既存指摘と対応

| Severity / 指摘 | 対応と確認 |
| --- | --- |
| P1: PC/スマホのブラウザ結果・視覚資料が0件 | 候補1をマネージャーへ引渡し済み。20分超の待機後も結果ファイルは未着。成果資料の取り込みをもって再評価する。過去のsandbox起動失敗をブラウザ成功とは扱わない。 |
| マネージャー視覚指摘: スマホ本文の魚が見分けられない | 文字倍率から本文魚の投影サイズを分離し、側方へ到達してから奥へ泳ぐ力と明るさを調整。実際の三角形を描画したcanvasを読み、本文等に隠れる領域を除いた可視ピクセルを検査。候補1の画像による判定は受領待ち。 |
| 前回P2: 解放後resizeで魚が画角外へ出る | 深さを固定し、側方の横倍率を現在幅/初回幅に分離する既存対策を確認。今回もGPU状態を維持する方式を保持。 |

## 残る確認とレビューの範囲

物理スマホ、Safari/Firefox、長時間の発熱・メモリー、本番の第三者スクリプト込みの動作は未確認。ブラウザ計装はViteレスポンスに限定され、本番コードへScene参照を公開していない。ピクセル面積の閾値は「描画されている」ことの補助であり、名前の判読性や遷移の自然さの視覚判定を代替しない。

この記録は独立AIによるコードレビューであり、人間の成果確認・承認を代行しない。push、公開、マージ、デプロイは実施していない。

実装担当追記: 指定の待機時間を超えても候補1の結果未受領。独立AIのコードレビューのみ完了し、視覚レビューは未完了。候補アプリ・テストは変更していない。
