# Static page for crawlers/LLM
/forllm    /forllm.html   200

# NOTE: SPA fallback (/* -> /index.html) is handled by wrangler.jsonc
# assets.not_found_handling = "single-page-application".
# Cloudflare Workers Static Assets rejects a catch-all 200 rewrite as an infinite loop.
